---
cluster:
  provider:
    oxide: Oxide
  cloudCredential:
    oxide:
      host:
        label: Host
        placeholder: "Oxide API host (e.g., https://oxide.sys.example.com)"
      token:
        label: API Token
        placeholder: "Oxide API token"
